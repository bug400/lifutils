Build the mac OS version of the LIFUTILS (pkg file)

Build requirements:

Xcode command line tools, cmake (download from https://cmake.org)

Build:
run build_lifutils.sh compiles the LIFUTILS and creates a pkg file. If this script is invoked with the
parameter "test" then the tests are run. Running tests requires a Python 3 interpreter.
