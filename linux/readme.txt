Build the linux version of the LIFUTILS (Debian package)

Build requirements:

cmake, debhelper, c development tool chain

Build:
run build_lifutils.sh compiles the LIFUTILS and creates a deb package. Note: this does not run the tests.
